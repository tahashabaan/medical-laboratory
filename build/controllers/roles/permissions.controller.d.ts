import { RequestHandler } from 'express';
import { SuccessResponse } from '../../types/responses';
export declare const getPermissionsHandler: RequestHandler<unknown, SuccessResponse>;
export declare const getLoggedUserPermissionHandler: RequestHandler<unknown, SuccessResponse>;
